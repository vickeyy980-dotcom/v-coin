import Link from 'next/link';import { requireUser } from '@/lib/auth';import { Nav } from '@/components/Nav';export default async function Assets(){const {profile}=await requireUser();return <main className="wrap"><h1>Assets</h1><div className="gridcards"><Link className="card" style={{padding:20}} href="/dashboard">V Coin</Link><Link className="card" style={{padding:20}} href="/crypto">Crypto conversion</Link><Link className="card" style={{padding:20}} href="/bank">Bank transfer</Link></div><Nav role={profile.role}/></main>}