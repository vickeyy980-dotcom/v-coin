'use client';import { useState } from 'react';import Link from 'next/link';import { useRouter } from 'next/navigation';import { createClient } from '@/lib/supabase/client';export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();setMsg('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error)return setMsg(error.message);r.push('/dashboard');r.refresh()}return <main className="wrap" style={{maxWidth:460}}><h1>V Coin Login</h1><form className="card" style={{padding:22,display:'grid',gap:12}} onSubmit={go}><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn">Login</button>{msg&&<p>{msg}</p>}<p className="muted">New user? <Link href="/signup">Sign up</Link></p></form></main>}