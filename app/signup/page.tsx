'use client';
import { useState } from 'react';
import Link from 'next/link';
import { createClient } from '@/lib/supabase/client';
import { PrimaryButton, TopBar } from '@/components/ui';
export default function Signup(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[username,setUsername]=useState(''),[name,setName]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function go(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const {error}=await createClient().auth.signUp({email,password,options:{data:{username:username.trim().toLowerCase(),full_name:name.trim()}}});setBusy(false);setMsg(error?error.message:'Account created. Check email if confirmation is enabled, then login.')}return <main className="flex min-h-screen flex-col px-6 pb-7 pt-3"><TopBar title="Create account" backHref="/login"/><form onSubmit={go} className="mt-6 flex flex-col gap-3"><Field label="Username" value={username} onChange={setUsername}/><Field label="Full name" value={name} onChange={setName}/><Field label="Email" value={email} onChange={setEmail} type="email"/><Field label="Password" value={password} onChange={setPassword} type="password"/>{msg&&<p className={`text-[12.5px] ${msg.startsWith('Account')?'text-green':'text-red'}`}>{msg}</p>}<PrimaryButton type="submit" disabled={busy}>{busy?'Creating…':'Create account'}</PrimaryButton></form><p className="mt-5 text-center text-[13px] text-muted">Already registered? <Link href="/login" className="font-semibold text-brass">Log in</Link></p></main>}
function Field({label,value,onChange,type='text'}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label className="block rounded-2xl border border-line bg-field px-4 py-3.5"><span className="mb-0.5 block text-[11.5px] text-muted-2">{label}</span><input required type={type} value={value} onChange={e=>onChange(e.target.value)} className="w-full bg-transparent text-[15px] text-cream outline-none"/></label>}
