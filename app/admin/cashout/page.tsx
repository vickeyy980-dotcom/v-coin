import {redirect} from 'next/navigation';import {requireUser} from '@/lib/auth';import {AdminCashoutOffers} from '@/components/AdminCashoutOffers';export const dynamic='force-dynamic';export default async function Page(){const{supabase,profile}=await requireUser();if(!['admin','super_admin'].includes(profile.role))redirect('/dashboard');const{data}=await supabase.from('cashout_requests').select('id,request_id,token_no,requested_vcoin,cashout_charge,total_debit,city,area,area_code,offered_city,offered_area,offered_area_code,offered_address,accepted_at,status,created_at,profiles!cashout_requests_user_id_fkey(username)').order('created_at',{ascending:false}).limit(100);const rows=(data||[]).map((x:any)=>({...x,username:Array.isArray(x.profiles)?x.profiles[0]?.username:x.profiles?.username}));return <AdminCashoutOffers rows={rows}/>;}