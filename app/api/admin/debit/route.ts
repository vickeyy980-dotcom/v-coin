import { NextResponse } from 'next/server';import { createClient } from '@/lib/supabase/server';export async function POST(req:Request){const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Not authenticated'},{status:401});const b=await req.json();const {data,error}=await s.rpc('admin_adjust_vcoin',{p_username:String(b.username||''),p_amount:Number(b.amount),p_direction:'DEBIT',p_note:'Admin debit',p_idempotency_key:String(b.idempotencyKey||'')});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json(data)}