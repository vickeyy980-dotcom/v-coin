import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase/server';export async function POST(req:Request){const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Not authenticated'},{status:401});try{const b=await req.json();const {data,error}=await s.rpc('admin_offer_cashout_location',{p_request_id:String(b.request_id),p_city:String(b.city||''),p_area:String(b.area||''),p_area_code:String(b.area_code||''),p_address:String(b.address||''),p_note:String(b.note||'')});return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json(data)}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}