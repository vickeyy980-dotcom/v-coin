'use client';
import { useEffect,useState } from 'react';
import { createClient } from '@/lib/supabase/client';
import { TxRow } from '@/components/ui';
type Row={id:string;transaction_id?:string;transaction_type?:string;direction:string;amount:number;created_at:string;description?:string};
export function LiveRecentActivity({userId,initialRows}:{userId:string;initialRows:Row[]}){const [rows,setRows]=useState(initialRows);useEffect(()=>{const s=createClient();async function refresh(){const {data}=await s.from('wallet_transactions').select('id,transaction_id,transaction_type,direction,amount,created_at,description').order('created_at',{ascending:false}).limit(3);if(data)setRows(data.map((x:any)=>({...x,amount:Number(x.amount)})))}const c=s.channel(`activity-${userId}`).on('postgres_changes',{event:'*',schema:'public',table:'wallet_transactions'},refresh).subscribe();return()=>{s.removeChannel(c)}},[userId]);if(!rows.length)return <p className="py-4 text-center text-[13px] text-muted-2">No transactions yet.</p>;return <>{rows.map(t=>{const received=t.direction==='CREDIT';return <TxRow key={t.id} href={`/history?tx=${encodeURIComponent(t.id)}`} direction={received?'received':'sent'} name={received?'Received':'Sent'} time={new Date(t.created_at).toLocaleString('en-US',{hour:'numeric',minute:'2-digit',month:'short',day:'numeric'})} amount={`${received?'+':'-'}${Number(t.amount).toFixed(2)} VC`}/>})}</>}
