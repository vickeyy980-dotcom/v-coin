'use client';import { useEffect,useState } from 'react';import QRCode from 'qrcode';export function QrCard({address,username}:{address:string;username:string}){const [src,setSrc]=useState('');useEffect(()=>{QRCode.toDataURL(JSON.stringify({type:'VCOIN',recipient:address,username}),{width:280,margin:2}).then(setSrc)},[address,username]);return <div className="card" style={{padding:22,textAlign:'center'}}>{src&&<img src={src} alt="Wallet QR" style={{width:280,maxWidth:'100%',background:'#fff',padding:8,borderRadius:16}}/>}<p><b>@{username}</b></p><p className="muted" style={{wordBreak:'break-all'}}>{address}</p></div>}